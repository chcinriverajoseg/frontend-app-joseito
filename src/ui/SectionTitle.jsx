import React from "react";

const SectionTitle = ({ children }) => {
  return (
    <h2 className="text-xl font-bold text-gray-800 dark:text-gray-100 mb-4">{children}</h2>
  );
};

export default SectionTitle;
